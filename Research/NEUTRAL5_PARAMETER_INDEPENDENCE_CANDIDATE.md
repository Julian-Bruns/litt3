# Candidate: the neutral first-repair line has constant next obstruction

2026-09-11. Proposed bounded geometric lemma, NOT yet canonical/proved.
It concerns the explicit R=u(u-1) D10 cover of
explicit_non_galois_neutral_five, with all the actual input data and
markings in PRO_NEUTRAL_DIHEDRAL_FOURTH_LIFT_REQUEST.md.

Let W→D be the original cyclic-five closure and T=W/<tau> its degree5
quotient over C. Put e=sigma-1, R=k[e]/e^5. The actual normal cokernel
on W is R/(e²), while O_T injects into O_W by the prime-to5 map W→T.
The full compatible third-lift line is T3(b)=T3ref+xi+b*h*kC.

Candidate conclusion:

    epsilon_T(T3(b))=epsilon_T(T3(0)) for EVERY b in k.

This would reduce the requested fourth-level zero set to all or none.
It would not compute the remaining scalar, prove a fourth lift, or
give full-tower descent.

## Actual cochain data available

All first curve classes on W can be represented by AS polynomials of
degree at most2: the particular xi has w-degrees0,1,2, and h*kC is
independent of w. On the actual cyclic torsor function algebra,

    e² A=P2, e^4 A=P0,
    P2*P0 subset P2, P0*P0 subset P0.

Here Pj denotes degree-at-most-j in w, using w^5-w=f. The translated
chart wO=w-chi preserves these subspaces. Fifth power, base differential
operators, reference maps and normal projection preserve them.

The pulled-back two-affine negative normal complex has an integral
deck-linear section and normal primitive: H1 is regular of rank6 over
W2[C5] and H0=0. Consequently the first Hodge-generator repair of an
exact e² error can be chosen in e² cochains; its difference when b
changes is in e^4 cochains. This is the actual unique first repair,
not only a cohomological class.

## Relative higher comparison to justify

Compare TWO compatible upper W3 tuples, not a supposed compatible
lower C3. For a common descended smooth reference use a genuine
auxiliary global filtered oper extending the already specified H1.
Such extensions exist because H1(omega²)=0. Its failure of inverse-
Cartier compatibility is retained in the reference error.

The existing corrected filtered-and-graded LSZ construction, through
the next output modulo125, should give a normal comparison consisting
of (i) a divided integral linear carry, (ii) additive next-digit
changes of the previous filtered object, and (iii) ordinary quadratic
products of the first curve/Hodge-generator corrections. The following
precision argument is intended to verify this list in this case.

In a weight-one oper frame the tilde matrices are

    Mtilde=[[a_new,5*b_old],[0,d_new]],
    nablatilde=5d+[[5alpha_old,25beta_old],[gamma_new,5delta_old]].

The new graded entries are prescribed, not obtained by dividing the
lower-left entry of an arbitrary graph lift. The actual previous H2
is known modulo25, so its first variation is5 times reduced cochains.
In these matrices its relevant changes occur additively at order25;
the beta variation is125 and disappears. Products of two first previous
repairs vanished modulo25 and are not resurrected by these formulas.
The genuine oper-frame Taylor operators have valuation at least j-1
before dividing by j!. Factoring binomial numerators into a variation
shows that terms nonlinear in a displacement5*zeta begin at order125
for this output; mixed changes with order25 tilde variation also vanish.
Thus previous scalar/frame feedback contributes additively at the
last normal digit, not a new divided quadratic carry.

For final output graph matrices R_i=I+5*l_i*E21 and
G=G0+5K+25L, the normal entry is

    5(K21+D*l_i-A*l_j)
     +25(L21+K22*l_i-K11*l_j-B*l_i*l_j) mod125.

Divide by25 only after repairing the first bracket. The divided
integral residual of that bracket must be retained; it is not legitimate
to divide inside e² and assert the quotient remains there.

The relative ordinary quadratic terms under b→b' contain at least
one P0-valued difference and at most one P2-valued first repair.
They therefore lie in P2=e²A and have zero class in R/(e²). The
additive next-digit terms on the P0-valued difference likewise vanish.
All coefficient Frobenius transports preserve the abstract deck action.

## The divided linear difference

The actual two-digit linear normal operator on the free lattices is
deck-equivariant and reduces to the actual Psi_W. After choosing
independent source/target unit blocks, its remaining scalar reduction
is e²*Phi, since the audited cyclic Smith length is2. No Fitting
simple-zero assertion on Psi_D is used here.

The leading relative curve direction is bdiff*e^4 times the source
kernel generator, with the ordinary coordinates adjusted accordingly.
For an integral lift of this direction the scalar linear residue is

    [(e²*Phi)(bdiff*e^4)/5] in R/(e²),

up to additive deck-equivariant next-digit terms whose input is e^4.
The latter are killed in the cokernel. The former is zero because

    e^5/5=-e-2e²-2e³-e^4,
    e^6/5=0 mod(5,e²).

One can equivalently lift the relative class as the actual norm of a
lower kernel class. Its leading lower linear image is zero, and its
divided next coefficient pulls back to the e^4 norm image, also zero
in the upper cokernel. This avoids choosing a nonexistent lower Hodge
lift; only the linear comparison on genuine auxiliary opers is used.

Combining these pieces would give zero relative next obstruction on W,
hence on T by prime-to5 injectivity.

## What the audit must decide

The actual first repairs, matrices and Smith lengths are already audited.
The new issue is whether the relative LSZ comparison really has exactly
the three kinds of terms above at this precision, with the stated
augmentation properties, when the lower C3 reference is incompatible.
In particular, check changes of prescribed graded identification,
normal-line transport, the integral cochain primitive and elimination
of the unit block. A merely abstract module calculation is insufficient.

If a term is missing, identify it and retain the full affine-line Pro
target. If the comparison is valid, the scalar P(0) is the only remaining
fourth-Witt computation; its value is NOT asserted here.
