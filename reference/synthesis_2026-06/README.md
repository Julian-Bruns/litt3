# June 2026 synthesis

`partial_progress_note.tex` is the coherent monolithic snapshot of the
conditional reduction.  `litt3_partial_progress.pdf` is its 17-page rendered
companion and contains no additional theorem.

Agents should use the root status and modular route files first. Load this
directory only to inspect provenance for an argument that has not yet been
modularized. The synthesis still leaves Open Theorems A-D and is not a
completed solution.

## Referee errata

The snapshot predates the active September 2026 referee pass. In particular:

- repeated-\(u^{20}\) note `100` is absent, and the scalar response
  identities used at the other repeated layers are also unreproduced;
- the entry-zero norm comparison used incompatible unit normalizations. With
  \(\pi=cd\), \(\mathcal N=(1-c)(1-d)\), and \(\alpha=z(P_0)\), the three
  norm constants are respectively \(-\pi\alpha^{31}\), \(-\pi\), and
  \(\mathcal N\). The tangent cone makes these formulas compatible but does
  not prove \(c=d=2\), so the specialized double-fiber tower has a new open
  entry/coverage gate;
- the basin point \(r_{129}\) has six displayed coordinates, while the
  parameter map to the 19-coordinate terminal point `P129` is absent;
- Cartier fixedness does give a rational logarithmic primitive, but matching
  the prescribed divisor is controlled by the class
  \([(R-\operatorname{div}(r_0))/5]\), not merely by principality of \(R\);
- the no-highpoint residual ansatz fixes substantive cross-values, and a
  plane resultant can detect a singular image point rather than ramification
  of its normalization; and
- the common-cover descent conditional on visibility is now proved in the
  active file `routes/global/10_PROOF_SELF_CORRESPONDENCE.md`, while the
  correspondence-to-over-orbifold envelope remains open.

These are status and logic corrections to the snapshot. The TeX and PDF are
left unchanged as dated artifacts and must not be cited over a conflicting
active route file.
