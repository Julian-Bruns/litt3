# Why the next target is the early cyclic125 bootstrap

2026-09-10. Prompt-selection record. Main pair unchanged; original
common-cover problem UNSOLVED. This is not an early125 theorem.

## Progress before selecting the request

The new D25 response was replayed and focused-audited. It is now
cyclic_twentyfive_delayed_descent v1, with all-level original-map
descent and algebraization. Separate application audit excludes all
actual Sylow25 C50/D50/C2×D50 carriers in the ordinary-X/source2/
Galois-Y/nontrivial-five-action branch for the SAME main parameter.

The root then extended the genuine auxiliary-oper argument to ALL
cyclic q=5^a at late stages n>=a+1. The new scalar-feedback recurrence
keeps every relevant digit, instead of importing only the first-digit
D25 formula. Fresh audit PASS; canonical cyclic_power_late_descent v1.
It proves GIVEN T_(n+a+1)→GIVEN T_(n+1) descent along original h,
provided compatible lower C_n is already given. It does not supply
the early lower lifts. At125 only n2,n3 lie before that induction.

Thus the next question is ONE finite bootstrap: does every GIVEN
compatible T7 above the initial marking have its GIVEN T4 descend to
a compatible C4 along original h? Positive answer starts the audited
late induction at n4 and therefore gives every full cyclic125 tower.
A finite-level counterexample would show that this precision is
insufficient and identify an actual surviving nonlinear channel; it
would not itself be a nonliftable infinite tower or common-cover example.

## New local tests, with proofs of their limited algebraic content

In the actual regular torsor function algebra modulo25, the root proved
and tested

    (e123 A2)^2 ⊂ e122 A2+5e22 A2,
    (e122 A2)^2 ⊂ e120 A2+5e20 A2.                       (1)

Let B_j(i)=binom(i,j) and P_s span B0..Bs. Integral wraparound is
eB_j=B_(j-1)-binom(125,j)B124. Mod25 the only nonzero wraps have
j=25,50,75,100 and are divisible by5, so at most one wrap survives.
This gives

    e^(125-c)A2⊂P_(c-1)+5P_(100+c-1), c=2,3,4,5.

For c2 or3, products consequently lie in P_(2c-2)+5P_(100+2c-2).
Match the reduction by the lifts e^(126-2c)B_(126-2c+j),
0<=j<=2c-2. Their reductions are B_j and their integral support has
the same upper bound. The residual divided by5 is therefore in
P_(100+2c-2), which is e^(26-2c)A1. This proves(1) and descends by
faithful flatness, including pulled-back coefficient lines.

verify_cyclic125_early_algebra.py checks all15,625 delta-basis pairs
for EACH inclusion via125 relative-shift pairs and translation symmetry.
The replay also computes the pure kernel first repair supported at
23,48,73,98, next repair starting at3, and third LINEAR residue -e.
The separate all-power mixed-additive script passes37 C125 cases with
noncommuting coefficient operators and higher norm digits (1,337 total).
These checks do NOT construct the early Hodge comparison.

## Why (1) does not already finish the bootstrap

At the initial stage the first graph repair is of order5, and a second
repair is of order25. Its square has order625, the final normal digit
of the third carry. The first linear carry permits the second repair
to lie in e23A1=P101, much larger than the first-repair P1.

There is even a SINGLE square in P101 outside eA:

    B100*B24=B124,
    [B124](B100+B24)^2=2 modulo5.                       (2)

All coefficients were checked exactly. This is stronger than observing
an arbitrary mixed product outside the filtration. It does not establish
that this cochain is an ACTUAL second Hodge repair. The bootstrap must
use the relations among the actual repairs, combined divided carry,
or a genuinely different geometric mechanism.

At n3 the first graph square occurs one digit before the last, with
first-times-second terms at the last digit. The curve-deformation
ideal 5^3/5^7 is not square-zero: quadratic curve gluing can feed the
last normal digit after the Frobenius-displacement division. These
terms cannot be dropped using the late theorem. Once the first lower
digit descends, the arbitrary next lower reference has its own eta0;
the old canonical reference is not silently substituted for it.

The new prompt supplies the all-power late theorem, full mixed algebra,
genuine auxiliary-oper method and(1). It asks for the actual finite
bootstrap, not a reproof of these known results and not an assumed
choice-independent polynomial in d. It retains the objective completion
list and quality-over-speed closing requested by the user.

## Prompting and scope checks

Checked canonical inventory and latest actual computation before drafting.
Used OpenAI Docs skill for structure, with current
[Astra prompting guidance](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices),
while retaining the user's preferred task list. No model/effort setting,
browser submission or runtime guarantee was introduced. Source scope
matches the skill; prior firsthand prompting reports remain in
PRO_PROMPTING_FIELD_NOTES.md and were not relabeled controlled evidence.

The prompt file contains only receiving-model text. The new task is
harder because it must eliminate actual coupled early repairs and
their divided products; its proof would deliver a whole next class
of towers, not just an isolated coefficient on one chart.
