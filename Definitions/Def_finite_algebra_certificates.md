# Exact finite-algebra computation conventions

Let R=K[z_1,...,z_n], I an ideal, and A=R/I a nonzero finite-dimensional
commutative algebra. Its length D means dim_K(A), NOT the number of roots.
A "known length" is proved independently of the candidate certificate
being tested. A monomial order is fixed whenever leading monomials occur.

A derived polynomial is an element of I obtained by exact ideal-preserving
operations. Numerical agreement at sampled points does not certify this.
Coefficient arithmetic, polynomial reductions, and matrix ranks are exact.

When K=F_q, write F_A(a)=a^q and N for the nilradical of A. This F_A is
F_q-linear. The p-power map is generally only semilinear if q is not p.
A closed point P has residue degree d_P=[kappa(P):F_q] and local length
ell_P=length(A_P). It contributes d_P*ell_P to D and d_P geometric points.
Each of these geometric points has multiplicity ell_P.

The "standard monomials" of a monomial ideal are those outside that ideal;
their number is its colength, possibly infinite. Solver S-pairs are
consistency checks between polynomial relations, not candidate roots.

These conventions do not presume that A is reduced or generated by a
single element as an algebra, nor that its roots lie in the ground field.
