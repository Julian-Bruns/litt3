# Arithmetic-count request: successful outcome and exact payoff

2026-09-09. The Pro request RETURNED a proof, not another undecided
reduction. Independent medium audit PASS, no blocking issue. Its useful
device is the degree-16 square-subgroup cover, which includes all
noncongruence and non-derived arithmetic genus-two lattices.

Canonical result: [arithmetic_genus_two_reduction_bound](../Theorems/Thm_arithmetic_genus_two_reduction_bound.md).
There are fewer than2^2000000 arithmetic genus-two classes, and no more
potential good geometric reductions at any fixed prime. All models,
places and residue conjugates are included. The proof uses standard
arithmetic bounds, not a classification computation.

The CURRENT prescribed parameter already exceeds this bound.
The resulting [version2 application](../Theorems/Thm_liftable_coreless_target_finiteness.md)
excludes every fully liftable actual common span for fixed X and Y_t,
even after common etale refinement or ramified mixed-characteristic
base extension. No curve change or A18 result is needed.

Hence every hypothetical common span is coreless, intrinsically
nonliftable, and has no indigenous-ordinary shared nilpotent connection.
An admissible active match nevertheless gives the common W2 lift. For
each fixed such span, its marked deformation ring has5 nilpotent with
finite exponent e>=2. We have no bound on e uniform over spans, and
no implication W2=>W3. A nonzero source defect is not a common endpoint
tensor, so it cannot simply be fed to the no-clump theorem.

Also distinguish NILPOTENT ordinariness from DORMANT reducedness. Every
dormant point is already nonordinary in the nilpotent scheme, whose
linearized equations vanish there. Thus the new result does NOT force
a shared dormant point to have nonzero dormant tangent. The extra
source-defect restriction is substantive for the ACTIVE branch.

This closes the liftable branch, NOT the whole common-cover problem.
Both remaining alternatives matter: nonordinary matching connections;
and no-clump spans with no common connection at all. Further work must
control one of these actual two-leg alternatives. Do not resend OW,
the completed arithmetic count, or a broad higher-level lifting question.

[Original request](PRO_EFFECTIVE_ARITHMETIC_REDUCTION_BOUND.md) is retained
as the record of what was asked, not as a pending prompt. No Pro request
is outstanding. A18 remains parked and all computation data preserved.
