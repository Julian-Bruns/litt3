# Current frontier — 2026-09-06, 03:56 CEST

LITT IS UNSOLVED, but a genuine ALL-DEGREE BRANCH EXCLUSION is now proved.
Do not restart. Read this first. Prior retained state/counterexamples:
[03:25 frontier](FRONTIER_2026_09_06_0325.md), including its genus17 supplement.
User offline, requests autonomous continuation, conservative specific
agents, significant proof checkpoints, hourly simple-language updates.

## NEW audited theorem: e=d forces a core, for all p>=5

[Shared simple-root tensors force a core](../../Theorems/Thm_shared_tensor_core.md)
independently audited PASS by fresh /root/contact_bound_to_core_audit,
2026-09-06. Audit link in theorem header; don't routine-open audit.

For ANY actual bi-etale span in char p>=5, if shared regular weight-d
tensors (p∤d) have uniform zero multiplicity e=d, the span HAS A CORE.
No Hom, Galois, or degree bound hypothesis. Both unbounded d and all
covering degrees are covered, because each hypothetical seed has FIXED d.

Key combination of old and new results: a coreless seed generates
infinitely many distinct jointly minimal images in the SAME X×Y with
unbounded degrees (retained generic-graph theorem). Equality of the SAME
endpoint tensors propagates along every alternating fiber-product path
and descends through minimalization by injective separable pullback.
Generated images need NOT be coreless. The contact/root bound applies
to ALL of them and contradicts unbounded degree. The constant in charp
is c_p=4p/(p-4), giving b<=c_p d(gX-1) for each image.

Thus earlier limits160,320,640 are WEAKER than this deduction, not a
remaining finite search problem for coreless spans. Quantitative contact
notes retained because they contain the actual proof input.

Secondary audited lemma: a CORED bi-etale span preserving a Cartier-fixed
regular ONE-form whose every zero has order<p-2 has positive-genus core.
Proof: simultaneous Galois ETale refinement W, finite group generated
by the two deck groups; shared form descends to rational Cartier-fixed
beta=dlog q on core. Poles at most simple. Tame pullback of a pole has
order−1; wild has order delta−e>=|I1|−2>=p−2, impossible. This uses the
correct mixed-inertia bound, NOT the false delta−e>=e−2 bound.

Corollary: if Hom(JX,JY)=0, no span can share a Cartier-nonzero one-form
with only simple zeros (core theorem + positive-genus core contradiction).
This addresses such cored spans too, but does not address arbitrary
nonuniform forms with larger zeros.

## Fixed pair consequence

X g9 / Y ordinary g25, Hom(JX,JY)=0, degrees a=3M,b=M as file76.
For a CORELESS positive canonical intersection k[s], primitive5∤d,
zeros uniform e. ALL e=d cases are excluded, in every weight.
In particular d2/e2 andd4/e4 are gone. If d1, ordinaryY makes the
Cartier scalar nonzero; audited genus9 uniform-zero certificate forces
e1, so the ENTIRE primitive-weight-one coreless branch is gone.

Remaining: A=k; e!=d, including nonzero d2/e1,d4/e2; other high
Cartier-zero weights; and cored configurations not otherwise eliminated.
Do not claim general corelessness of all candidate spans.

## Running exact strengthening test (potential next useful result)

Question: do ALL nonzero Cartier eigenforms of file76 X have simple zeros,
not just the ones with UNIFORM zeros? If yes, combined with the theorem
above and the secondary lemma, no span on fixed pair can share a nonzero
regular ONE-form at all. This is not proved yet.

Existing normalized three-variable Cartier equations are

    U=(u,v,1), U^25=c^2 H U, H=N*Mr overF25,
    AA=x²+v5 x+u5, LL=sum_j(Mr U)_j x^j,
    norm= c LL³+F AA³ (monic degree16).

New exact observation: O with rows e3,e3H,e3H² has determinant a+4!=0.
Therefore every nonzero eigenform lies in this A-degree-two chart:
an eigenvector with coordinate2 zero would be annihilated by all rows O.
H coefficients lieF25, so raising25 commutes with it.

BETTER UNIVARIATE PARAMETRIZATION (root derivation, not yet audited):

    Pc(c)=det(H)c^1302 - charpoly(H)[1] c^1300
           + trace(H)c^1250 -1,
    U=O^(-1)*(1,c^-2,c^-52).

Pc is separable (derivative2det(H)c^1301 and constant−1). The full
three-variable equations are equivalent: apply O to U^25−c²HU;
the first two entries cancel directly, third by Cayley--Hamilton/Pc.
This gives a finite algebra of degree1302. Factor Pc overF25, work in
each quotient field, compute gcd(norm,norm') in degree16. Squarefree
norm on ALL factors suffices to prove all eigenforms simple. A repeated
norm root can also mean distinct simple zeros sharing x-coordinate, so
failure would need further local checking, not immediately refute claim.

TWO calculations currently running:

* older four-variable Groebner test, exec session41103; processPID7090
  (parent7088), command uniquely recorded in process listing. It adds
  norm(x)=norm'(x)=0 to the three equations; may be much slower. No
  result yet. It may be stopped once the better test gives an answer.
* improved univariate test, exec session67074, script
  GENUS9_CARTIER_EIGENFORM_SIMPLE_ZERO_TEST.sage. It temporarily loads
  old even-zero script to get constants (6.6sec), then factors Pc and
  prints gcd degrees. Not polled yet at this checkpoint.

If improved test succeeds and is audited, consolidate into one standalone
certificate and strongest theorem, then remove obsolete special-case
proof/certificate as user requested. Don't delete useful original input
before verification. Sage regenerates .sage.py files; these are disposable.

## Important counterexample boundary retained

Audited genus17 partial Igusa P with Cartier-fixed alpha and SIXTEEN
DOUBLE zeros has jointly minimal CORELESS Hecke spans degrees
12*11^(n−1), all n>=1, on the SAME fixed P. Thus universal double-zero
degree bound is FALSE. The new quotient surface Q' is klt ampleK but
contains unbounded such leaves at2g−2=(10/17)K.Gamma. Extra fixed-endpoint
data are required, not just more general-type/cotangent inequalities.

Additional ONE-ENDPOINT exact stress test appended to that counterexample
note (root certificate, not independently audited): C0:y²=x5+x3+2x+2,
genus2 ordinary absolutely simple Jac, alpha=dx/y double zero∞ and
C(alpha)=4alpha. Counts5,31, Weil T4−T3+3T2−5T+25. Root-ratio resultant
factors into(z−1)^4 and two nonintegral monic irreducible quartics, each
irreducible by reduction toPhi5mod2 after clearing denominators. Hence
no root-of-unity ratio, absolute simplicity. This refutes the ONE-ENDPOINT
repair 'double-zero eigenform must come from an elliptic quotient'; no
second leg on C0 is asserted. Three tiny Sage verification sessions
55584,3426,93799 all completed; don't poll them.

## Operations

No active agents; all relevant audits completed. Timer122 remains
asynchronous, due04:24CEST. No browser/Pro request. Do not end at this
local milestone; user requests continuation. update.md refreshed early
for major all-degree progress; scheduled next hourly checkpoint unchanged.
