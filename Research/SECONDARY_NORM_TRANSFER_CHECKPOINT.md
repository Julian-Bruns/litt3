# Why the next Pro target is the W5 constant component

2026-09-10. This is prompt-selection evidence and research state,
not a proof of the requested transfer formula.

The uniform returned D5 argument yielded an audited initial case and
an audited all-level case WITH an existing compatible lower reference.
The common-cover application still needs lower-reference existence.
Reasking uniform D5 would repeat most of the preceding response.

The new prompt asks for pi0(epsilon_T(T4)) for actual compatible upper
T4 over an arbitrary compatible bad-double C3. Its free first upper
parameters are c e²+d e³+b e^4, with c^5=eta0. The constant coordinate
is the remaining possible detector of the lower obstruction eta0.
The other unwanted direction is already controlled when eta0=0.
Computing this FUNCTION, including its parameter dependence, provides
information even if it is not an invertible transfer; it is not another
kernel/stable-image reformulation of full two-leg lifting.

Local mechanism checked before drafting:

- Equivariant Cech primitives really preserve e², at cochain level;
  the scoped medium audit confirms this new part of the Pro proof.
- The integral matrix carry really is -eta0(1+2e)-d^5e, coefficient
  determinant1; scripts/verify_cyclic5_integral_carry.py checks it.
- A non-gluing previous Hodge overlap has lower entry 5^(n-1)r,
  which weight rescaling DIVIDES by5. This invalidates using the
  same triangular-reference order estimate without a comparison.
- Exact graph polynomial arithmetic gives, at n=3, 5L+125Q mod625;
  the quadratic Q can survive. At n>=4 that raw quadratic vanishes
  at the corresponding precision. The standard-library check
  scripts/verify_incompatible_hodge_graph.py passes, and the same
  focused auditor independently confirmed the formula and scope.

No claim that Q survives the actual normal projection: that is part
of the requested computation. No use of the earlier unaudited numeric
beta polynomial. No assumption that tau lifts to arbitrary C3.

Inventory searches for Bockstein/secondary returned no existing
canonical result; the stronger compatible-reference theorem is
explicitly supplied. Exact underlying data and full previous-flow
conventions are included in the model-only prompt. No new solver or
exploration agent was started.

After the user submits that one prompt, resume the independent actual
cored span X←Z→Z/ker(defect) idea from STATE, not this same local
calculation or A18.
