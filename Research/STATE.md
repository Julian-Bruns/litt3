# Continue here — resource-limited structural research

Updated 2026-09-08 08:34 CEST. Original common-cover problem: UNSOLVED.
Preserve BOTH actual finite etale maps from the SAME projective source.

## Resource policy

All three ordinary agents have stopped with handoffs. The latest permitted
fresh medium audit has also finished. No additional agents except a
MEDIUM audit of a major genuinely uncertain theorem. At most ONE CPU
core TOTAL while the user sleeps. No heavy solver is running. No automatic
chat/CLI wakeups, speed changes, or computer-use Pro submission.
Read Research/PRIORITIES.md before choosing another branch.

## Last substantial result — completed, do not recompute

backup_hermitian_atlas_exclusion v1 is AUDITED PROSE, PASS by fresh
/root/audit_backup_atlas_bridge_medium on2026-09-08. The small genus-two
C_alpha has NO representable finite etale atlas to [Fermat6/PGU3(5)],
including every cubic determinant character: all five dormant opers,
all81 torsion classes, all405 pairs, all20 original projective charts.
The last three charts have exact factored ORIGINAL13-row unit identities;
the independent replay checks polynomial identities, not just a quotient
or a solver's empty result. Root completed the last two length8 algebras
in24 seconds each on one core. Audit covers the geometric bridge as well
as candidate completeness, finite precision and certificate composition.
See canonical theorem/proof and Research/BACKUP_CANDIDATE.md; the compact
receipt is computations/backup_genus_two_completion_status.json.
Large certificates are outside Git in litt3-computation-data/backup-genus-two.

This excludes both large Hermitian common-orbifold cases for X,C_alpha.
The Hessian216 quotient also excludes tame(24;3,3,4), using the existing
author-prose signature. New radical_quadratic_atlas_obstruction excludes
(16;2,4,8) and(36;2,3,9). Current backup counts:17/24 tame,9/12 small-wild,
2/2 large excluded. Seven tame and three small-wild cases STILL OPEN.
No no-cored theorem for the small backup, and NO coreless exclusion.
Active pair remains genus9 X / genus25 Y; C_alpha remains BACKUP.

## Current structural frontier and next action

NEW AUDITED FOUNDATION (08:22): inertia_generated_core_preserving_refinement
v2 PASS /root/audit_inertia_refinement_medium. Galois endpoint covers
whose inertia normally generates the FULL group have no etale intermediate
cover, even non-Galois. They are linearly disjoint from ANY original
etale leg. Matching completed extensions therefore preserve both upper
etale maps AND corelessness, without coprimality with the original degrees.
PERFECT auxiliary groups also preserve the EXACT primitive weight, by
excluding cyclic Kummer subextensions; regularity downstairs follows
from the tensor POWER identity, not from ramified pullback alone.

PSL2(F7), degree168, realizes uniform inertia n=2,3,4 over ANY nonempty
reduced divisor on ANY genus>=2 curve in char5. SGA1 XIII2.12 was read
including its exact inertia realization. Nine frozen surface tuples,
full inertia normal closures and perfectness replay0.017s; auditor0.018s.
Use n=3,2,4 for Cartier residues E=1,2,3. This turns EVERY positive
Cartier-zero generator into shared globally transverse dormant data,
retaining corelessness AND weight, with degree168 vertical maps.
Each actual upper leg degree grows by at most168. No simultaneous
lifting of the original maps: SGA1 only constructs individual auxiliaries.

LIMIT: the new genus is168(g-1)+84(n-1)r/n+1 and r is unbounded.
This is NOT A18 on the original endpoints and NOT a Tango=>core theorem.
The old audited solvable/coprime construction retains its useful ability
to avoid prescribed primes; do not delete it as a special case.
NEXT: exploit the retained degree168 PERFECT-GALOIS descent to original
endpoints, or the no-clump alternative. A generic Tango rigidity claim
is already false, including primitive-weight-nine examples; do not repeat.
Do not start a new solver just to enumerate higher-genus dormant opers.
The first downstream screen found NO extra independent endpoint
condition: descending the resulting Tango line after its inertia twist
returns the original compressed Cartier-root datum. Perfectness protects
weight but does not make the horizontal solution coefficients constant.
The weight-nine retained counterexample blocks the latter shortcut.
Do not spend another cycle rediscovering either point. The next useful
result must constrain the ACTUAL same-source comparison, not merely
regularity/transversality on the auxiliary curves. A second Pro request
has not been manufactured for this still-unfocused gap; the independent
generic-family singleton request and cored-formalization prompt are ready.

NEW HIGHEST-IMPACT FINDING (07:54): uniform_cartier_root_locus v2
AUTHOR is complete. For ordinary genus2 every uniform Cartier-zero
profile r is encoded by(D,theta), Vtheta=[D-rO], theta PRIME-TO5,
with M=O(jO1)theta^a, ar=2mod5, j=(ar-2)/5. Cartier must kill the
SPECIFIED section of O(aD), not merely some section. This removes all
unbounded tensor weights. Minimal weight=(r/gcd(r,2))*ord(Vtheta^e0).
T_r is finite-etale25 over Sym^r and a projective bundle over J1 for r>=3.

This exposes a REAL limit: r=5k+2, k>=1, has a P^(4k-1)-bundle of
Cartier roots over a nonempty OPEN subset of J1, with reduced divisors
and actual prime-to5 roots of UNBOUNDED minimal weights. Proof uses
Cartier-surjectivity at theta0 from ordinarity, relative kernels, and
generic exact meromorphic differentials with simple zeros. Thus no
independent genus2 endpoint sieve can exclude all larger clumps.

Even more concrete: on ANY subcanonical curve g>=3 with
div eta=(2g-2)O, general f in L((5g-6)O) gives
s=(df)^2 eta^5, weight7, div s=2D, D reduced degree7(g-1), Cartier-zero
because s^3=df*((df)eta^3)^5. The g2 version is
alpha=(u-b)du, s=alpha² eta^5, D=five finite Weierstrass points plus
the two points over b. These are NOT proper powers. Thus fixedX,
backupC_alpha, and fixedY independently pass the SAME weight7/e2
profile, with r=56,7,168. They are NOT an actual common cover.

If such tensors match in an ACTUAL coreless span, its shared generator
is primitive weight7 and the intrinsic rational connection has double
pole3/4 at every zero. Localization in k[s] proves that no other
shared connection cancels it. Hence this hypothetical class lies
OUTSIDE every common-regular-oper computation. Do not restart A18 to
address it, and do not promote independent endpoint existence to a span.

NEXT CONCRETE TWO-LEG TEST: fix canonical frames eta_X,eta_C and exact
forms alpha_i=df_i as above. If their weight7 tensors match, put
A=f*alpha_X/g*alpha_C, B=f*eta_X/g*eta_C. A²B^5=1 implies, with
h=A B², A=h^5 and B=h^(-2). Thus
f*df_X=h^5 g*df_C and f*eta_X=h^(-2)g*eta_C ON THE SAME SOURCE.
Integrating the first gives f*f_X=h^5 g*f_C+v^5. The second equation
is essential; the additive Frobenius relation alone loses the spin/
canonical compatibility and has retained counterexamples. Investigate
this exact two-form system, or a stronger unmarked two-leg necessity.
It is a test profile, not a claim that every span has weight7 or a clump.

Literature check: Krishnamoorthy ANT2018 Question9.7 explicitly asks
whether every proper coreless etale correspondence in charp has a
clump. Root reread the primary text; no resolution found in a targeted
search. Do not treat clump existence as an available theorem. A possible
ordinary-endpoint/Cartier-zero rigidity statement is NOT yet justified;
do not send a Pro prompt for it before testing higher-residue-field
quaternionic Hasse invariants. Banerjee2014 refers to Kassaei2004 for
the q-Hasse construction, but compatibility with canonical tensors and
ordinarity of the BASE Jacobian have not been verified.

coreless_connection_spectrum v2 is registered AUTHOR PROSE. The retained
actual genus17 partial-Igusa coreless Hecke spans have a shared one-form
with double zeros, and NO shared REGULAR projective connection. Proof:
all shared rational connections are r_alpha+k alpha^2; a zero of alpha
of order e has double-pole coefficient e(e+2)/4. For e=2 in char5 this
is nonzero. Thus universal common-connection existence is FALSE, not
merely unavailable. The example has a clump; it does not decide universal
clump existence or the endpoint-specific fixed-X/Hom-zero case.

The highest-impact gap remains an endpoint-specific obstruction to
arbitrary coreless spans. Do not replace it with universal Tango, Prym,
two-oper, simultaneous-Galois, or lifting assertions contradicted by
retained examples. Rank-four Frobenius pushforward is functorial but no
canonical matching rank-two Sym3 root has been proved.

The bounded triangle branch is COMPLETE: a=t^-1(t-1)^(-5/4) and
t^-1(t-1)^(-4/3) give actual etale J[4]/J[3] twisted quadratic tangents
for248/239. Existing J[4] tests and four new cubic unit minors exclude
them. scripts/backup_genus_two_cubic_tangents.sage generated2.368s and
--verify replayed2.373s, all400 nontrivial pairs, original operators.
No new solver or repeated census is needed.

NEW ACTIVE CORELESS ROOT SIEVE: singleton_cartier_theta_bound v1 AUTHOR.
For ANY ordinary genus2 curve in odd charp, T=C x_(J,V) J(C^(1)) along
i(P)=2[P-O] is connected etale degreep². Its Raynaud theta pullback has
degree8p(p-1). The six Weierstrass fibers contribute6(p²-1) forced
nonzero V-kernel points. Subtracting them ONCE leaves an effective
residual divisor of degree2(p-1)(p-3), hence16 for the backup. Every
singleton Cartier-ZERO canonical root datum of ANY prime-to5 order
comes from a NONBRANCH point(P,M) there with M prime-to5 torsion.
Conversely such a point gives actual etale one-endpoint root data.
Weierstrass points never work for prime-to5 M. R may be nonreduced or
still meet forced points. No emptiness claimed. At most8 abscissas;
each residual Frobenius orbit degree<=16, NOT a common degree16 field.
This removes dependence on the unbounded cover degree or torsion order.

THE SINGLETON QUESTION IS NOW SOLVED LOCALLY. The unsent Pro prompt
was removed as obsolete; do not reconstruct or submit it. New theorem
backup_singleton_root_exclusion v1: NO nonbranch(P,M) with V(M)=2[P-O]
is in Theta_B, even allowing five-primary M. Its proof uses
h=(A+vB)/(u-b)^2 and exactly THREE exhaustive charts: D avoids O/P;
D contains O; D contains P. All three have original polynomial unit
identities. Source backup_singleton_root_exclusion.sage defaults to
no-solver replay and passed in0.535s. Full geometric bridge/coverage
PASS fresh medium audit; auditor did not run computations. Root replay
checks complete Cartier kernels against original matrices and all unit
identities, with negative mutation control. Files singleton_norm_*.json
total2.18MB. NO residual enumeration remains: length16 stays as extra
multiplicity at the144 forced Weierstrass/nonzero-kernel points.

IMPORTANT COMPUTATIONAL FAILURE CAUGHT BEFORE PROMOTION: default dense
linear algebra over the specified non-Conway GF125 produced right_kernel
bases NOT annihilated by their input matrix. The earlier open/O unit
outputs used invalid parametrizations and were discarded/replaced.
Corrected generic arithmetic with explicit full-kernel checks generated
the open certificate13.599s and O certificate0.065s; P never used a
kernel parametrization. No known prior theorem was invalidated. Require
original-matrix replay for any finite-field kernel used as a COMPLETE
parametrization; never infer correctness from its reported dimension.

NEW COMPLETE: backup_raynaud_quadric v1 AUTHOR. The24 nonzero5-torsion
classes give12 Kummer points whose quadric evaluation has rank9. The
literal Q coefficients are in its statement, proof and revised Pro prompt.
Tong1.2.3 supplies LINEAR equivalence and Kummer descent, so Q is the
ENTIRE inverse-coefficient-Frobenius theta equation, not a sample fit.
For m on J(C), Theta_B(Fr(m)) iff Q(kappa_C(m))=0, VFr=[5].
Frozen scripts/backup_raynaud_quadric.sage checks all dlog functions and
rank/coefficients in0.286s. Its actual kernels already used generic
arithmetic and its differential equations were independently checked;
it is unaffected by the separate small-field kernel failure. Real
branch-point common factors of A,B occur: cancel them only in the sheet
ratio, never discard these classes. No ordinary agent or heavy solver.

NEXT: larger clumps/no-clump, not another singleton computation. Saved
nonzero-Cartier torsion/eigenform argument already excludes singleton
clumps in the complementary branch, so ALL singleton clump images on
C are now ruled out. Size2 forces a core by shared_tensor_core; size3
is prohibited modulo5 by cartier_generator. A remaining clump has C
image at least4. The common-connection criterion is ALREADY proved in
coreless_connection_spectrum: e=0 or-2d mod5, hence C image4mod5 here.
Do not rediscover it. Likewise automatic B_C theta^-1=Sym³(Q) and the
failure of rank-two matching are ALREADY in the retained source proof
routes/global/DORMANT_OPER_ORDINARITY_DOES_NOT_SUPPLY_TWO_LEG_MATCHING.md,
Sections1-2; root reread it. Regular affine relative Sym³ invariants
collapse to identity, spectral_dormant_prym_lines Section6. Main gap is
still endpoint-specific control of arbitrary coreless spans.

A bounded generic-family extension was attempted: use the SAME three
charts over F5(t), for the exact high-prime-degree partner family in
bounded_atlas_partner_finiteness. Chart1's generic Groebner call hit
the60s cap, with no decision; do NOT infer generic emptiness from the
special alpha fiber. No repeated solver retry planned. The focused
Research/PRO_GENERIC_SINGLETON_REQUEST.md is ready, NOT SENT: it asks
for generic exclusion with specialization denominators, or a genuine
divisor/Cartier counterexample (spurious norm solutions do not suffice).
This is independent of root's next larger-clump/no-clump reasoning.

NEW STRUCTURAL IMPROVEMENT (06:45): raynaud_genus_two_determinant v1
AUTHOR registered. On any genus2 char5 curve with the rank3 Cartier
coefficient map, H0(B_C M) is the kernel of a FOUR-square explicit
matrix in the Mumford coordinates of M on C^(1). It uses AF² reduced
by U^5=(u^5)^2-Su^5+P and four residue classes modulo5. It keeps
the actual chosen sheets, unlike the norm-only polynomial systems.
The full family C_t has an explicit Raynaud SUPPORT quadric Q_t,
all ten polynomial coefficients saved in theorem and revised Pro prompt.
Generic symbolic identity took0.132s. Rank minor3(t+1)^4; basis
denominators t²(t+1)^3; specialize valid for ALL t outsideF5. Exact
specialization agrees with independent backup quadric coefficients
raised to5 (because formula is on C_t^(1), parameter t^5). Global
support equality uses that every removed boundary divisor contains0,
where both theta and Q_t are nonzero. No multiplicity equivalence or
generic-family singleton exclusion claimed. Keep the independent backup
quadric theorem until this generalization is audited.

THIS IDEA IS NOW PROVED AND COMPUTED: raynaud_genus_two_determinant v2
AUTHOR extends to EVERY odd p and m>=1. Theta(M^m) has a(p-1)m-square
EXACT kernel on a nonbranch reduced degree2 Mumford chart. Repeated D
and m divisible by p are included. A-space dim(p-1)m follows from an
explicit minor equal to the Hasse determinant; no F(0)=0 assumption
is actually necessary. Original full-matrix checks48cases,p3/5/7,m1..4,
including repeated support, PASS0.49s. Code check_theta_power_matrices.sage.

Principal V-theta has a(p+1)-square exact model; keep its first TWO
residue(p-1) equations instead of choosing a possibly incomplete kernel.
For p5 the6-square determinant gives an explicit16-term CUBIC on Kummer,
source verschiebung_genus_two_cubic.sage, exact frozen identity0.13s.
Literal coefficients in solution Section5. This cubic criterion is only
on Res(U1,F1)Disc(U1)!=0; global boundary extension NOT claimed.
Both charts retain actual sheets. No generic intersection exclusion.

Concrete generic-family continuation: substitute N=3M-Fr_J([P-O]),
so M=2N and V(N)=[P-O]. Then use cubic E_t(N)=0 and theta_B(2N)=0
(eight-square power matrix, or Q_t after doubling). This is a two-locus
intersection ON the SAME Kummer surface. Forced Weierstrass points and
Mumford boundaries must still be retained. No intersection solver run.
Save this in the Pro prompt, then prefer the higher-impact larger-clump/
no-clump obstruction rather than repeating a singleton solver locally.

Elementary triangle containments to verify before use:
 S(2,4,8)->S(2,3,8) degree3 via S(4,4,4)/C2 -> /S3;
 S(3,4,4)->S(2,4,6) degree2 via z->z^2.
These can reduce duplicate future targets; no exclusion claimed from
the containments alone. No new Pro prompt is outstanding.

## Parked work — exact continuation points

1. Main genus9 census COMPLETE AND AUDITED:28990 distinct, length29375,
   18 untwisted representatives. Do NOT enumerate again. A14/A18 are
   AXIOMS ONLY. Main controller26046 and descendants STOPPED.51accepted,
   64calculated,0whole. Authority is external atlas-all18/all18.json.
   Research/ATLAS_OPERATION_LOGGING.md has the one-core handoff. Orbit6
   export hit PARI unpickle segmentation fault in multiprocessing result
   handler; five blocks saved, no chart manifest. It was not algebraic
   nontermination. Do not restart parallel without repair. Serial path
   bypasses that thread but has NOT had a fresh regression test.
   All18+8tower field fixtures PASS. Bulk checkpoint I/O remains untested
   and undeployed. All saved certificates/checkpoints preserved.

2. inverse_cup_atlas_system v2 registered AUTHOR PROSE: all18 use inverse
   mixed Bezout D=[[B,q],[a,0]], size24/27. Six exceptional cup ranks21;
   in dual bases a=q^T, a3-minor gives symmetric21-square P^T B P.
   Log gradient MUST retain2*dlog(det q0). Necessary72x32 condition
   Gamma(beta)q(U)=0 could exclude an exceptional oper if uniformly
   injective on the required stratum; this is UNPROVED. No runtime or
   emptiness claim. No need to reactivate library agent.

3. The returned Pro theorem weierstrass_pair_interpolation v1 is AUTHOR:
   135 relevant pairs all fail, at mostONE Weierstrass point, complete
   nonsingular9x9 chart coverage after relabelling, eight sheet-correct
   norm equations. Full residual pair surface still OPEN. Source replay
   PASS0.32s. Earlier double-support exclusion also author, replay45.878s.
   One-Weierstrass pair_matrix.sobj saved externally; generic solve stopped
   at118s, no exclusion. Do not retry M.solve_right. Fraction-free cubic
   algebra Bareiss is a concrete deferred alternative, not an active run.
   W3 classification does NOT exhaust arbitrary Pic(X)[3] twists.

4. Complete no-CORED prose target already exists for fixedX and the
   HIGH-PRIME-DEGREE genus2 partner, bypassing all atlas computations.
   Research/CORED_FORMALIZATION_REQUEST.md is READY to paste, with exact
   dependency slices in CORED_FORMALIZATION_MAP.md. No Lean installation,
   verification or mathlib-support audit performed. Not the small C_alpha.

## Durable bookkeeping

Last update06:08CEST. Passive timer86399 started about06:03, due07:03;
no CPU loop or automatic chat restart. All ordinary agents and the
two permitted bounded audits have finished. No heavy computation.
No CPU loop or automatic restart.
Root owns registry/state. Audit files reference-only unless concrete doubt.
Large-data Git exclusion complete; no push, raw deletion or publication.
Preserve all dirty agent work. No further ordinary agents under current policy.
