# Exact Frobenius graph: one bounded native-F25 pilot

2026-09-07. No basis was obtained. The graph formulation lowered the
working degree from7 to3 but did not complete in the same180second bound.
It used more memory than the previous two native pilots. No further
solver launch or retry is proposed by this test.

Add32 genuinely independent variables c_j, with the REQUIRED equations
c_j-b_j^5=0. The graph system consists of N(U,c)=0,R(U,c)-b=0,U.b-2=0
and these32 graph equations:129 equations in96 variables. Eliminating
c through its monic defining equations recovers every original equation
exactly, so this is the same affine scheme, including nilpotents; it is
not a relaxation and imposes no field equations on unknowns.

For each of the24 diagonal entries, add the certified cubic equation
(B(U)Gamma(c))_aa-1=0. Its ORIGINAL graph-generator identity is

    (B Gamma(c)-I)_aa
      = sum_(i,r) d_(a,i,r) U_i N_r(U,c)
        + [sum_i U_i(R_i(U,c)-b_i)+(U.b-2)]/2.

The source hashes match the saved complete inverse-cup certificates.
All24 by16896 coefficients in U_i U_j c_l were checked by decoding the
saved multipliers and multiplying by the original coefficient matrix.
The degree-two U.b terms cancel exactly, leaving the displayed constant
minus1. The graph relations themselves require no multiplier in this
identity. The original97 equations were regenerated byte-for-byte before
rewriting, and exact substitution c=b^5 was checked for all97. An
independent Singular evaluation at a point respecting c=b^5 verified
all153 exported equations and the nativeF25 field convention.

Input:96 variables ordered u0..u31,b0..b31,c0..c31, nativeF25, dp order;
97 quadratics,32 degree-five graph relations and24 cubic diagonal seeds.
The diagonal seeds contain272,707 terms. Input size is6,398,950 bytes.

| Pilot | Working degree at stop | Wall time | Sampled peak RSS | Basis bytes |
| --- | ---: | ---: | ---: | ---: |
| Original nativeF25 |7|180.016sec|1,415,331,840|0|
|32 off-diagonal degree7 seeds |7|180.031sec|1,394,032,640|0|
| Frobenius graph plus24 diagonal seeds |3|180.044sec|2,293,858,304|0|

The final graph log contains degree3 matrix batches, latest completed
internal counter22092, but no `BASIS_COMPUTATION_RETURNED` or
`OUTPUT_WRITTEN` marker. Counters and working degrees across different
presentations do not measure distance to completion. The observed
lower degree therefore cannot be interpreted as demonstrated progress
toward a completed basis.

The whole solver process group's RSS was sampled every0.5sec with a4GiB
kill threshold. Only one member was observed. The threshold was not
reached; the wall guard terminated the solver, subsequently confirmed
absent. This is sampled monitoring, not a kernel-enforced hard memory cap.
The forced exit returned0, explicitly NOT success. No output basis exists
to verify, and no geometric exclusion follows.

The exporter and guard gained `--graph` options rather than duplicate
utilities. All former input/log/run evidence remains unchanged. New
artifacts use the prefix
`Research/computations/canonical_atlas_graph_native` with `.sing`,
`_check.sing`, `_export.json`, `_pilot.json`, and `_pilot.log` suffixes.
Export command was `sage scripts/export_canonical_atlas_seeded_native.sage --graph`.
The once-only guard was `python3 scripts/run_canonical_atlas_seeded_native_pilot.py --graph`.

The main thread subsequently retired all unsuccessful solver prototype
export/launch code and these exact input/checker files recoverably to
`/Users/julian/.Trash/litt3-atlas-pilots.sucrLn`. The commands above record
the historical run, not active recipes. Tensor/certificate mathematics,
logs, hashes and status records remain in the workspace. No solver runs.
The unmarked common-cover problem remains unsolved.
