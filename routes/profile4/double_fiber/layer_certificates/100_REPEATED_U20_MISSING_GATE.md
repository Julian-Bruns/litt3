# Missing repeated-\(u^{20}\) gate

## Status

**missing dependency / open**.  This is a guardrail, not a replacement
certificate.  The source formerly called note 100 and every cited executable
artifact are absent.

This target belongs to the conditional \(c=d=2\) formal tower. Closing it
would not prove that the general entry-zero branch has that specialization,
or justify any additional \(\alpha^{31}\) or \(K\) normalization.

## What must be supplied

Starting from the incoming graph after the simple-\(u^{20}\) solve in file
97, write the complete repeated-\(u^{20}\) affine residual in its new tail
variables.  On the priority pieces

\[
D(\Delta\rho_3),\qquad
V(\rho_3)\cap D(\Delta\rho_4),
\]

prove the precise rank and solvability statement needed to construct the
incoming graph assumed by file 101.  The proof must:

- display all response columns and any Schur correction from the simple
  layer;
- name the pivot minors and every inverted factor;
- show that the rank statement holds fiberwise over all older free
  coordinates, not just on a zero slice or sampled \(\mathbb F_5\)-points;
  and
- state the resulting free-variable space passed to the \(u^{25}\) layers.

A written derivation or an exact executable certificate with all input
polynomials is acceptable.  Analogy with repeated \(u^{15}\) or
\(u^{25}\), and finite sampling, are not proofs.

Until this gate is closed, files 101, 104, and every \(u30/e30/e50\)
calculation are downstream conditional analyses only.
