# Backup genus-two Hermitian atlas bridge audit

Verdict: PASS, scoped to the absence of a representable finite etale atlas
`C_alpha -> [H/PGU_3(5)]` over an algebraic closure of F5, where
`C_alpha: v^2=u(u-1)(u-2)(u-3)(u-alpha)`, `alpha^3+alpha+1=0`,
and `H: X_0^6+X_1^6+X_2^6=0`.

Auditor: fresh bounded agent `/root/audit_backup_atlas_bridge_medium`.
Date: 2026-09-08. Verification: independent prose/code mathematical audit,
not Lean verification. No material mathematical objection found.
No agents were spawned and no solver or heavy computation was run.

Inputs read: current continuation files; canonical statements and the
proofs of `intrinsic_atlas_incidence`, `dormant_rank_two_candidates`, and
`rooted_atlas_charts`; `hermitian_atlas_extension_criterion` statement;
`backup_genus_two_tensor.sage`, `backup_genus_two_finish.py`, preparation
and torsion generator sources, both original-row replay implementations,
the relevant parts of `BACKUP_CANDIDATE.md`, five saved tensors, and all
twenty chart replay receipts. Existing audited abstract atlas equivalences
are dependencies, not re-audited from their old audit bodies.

## Exhaustion of actual geometric candidates

The complete regular connection chart has three parameters because its
differences are all of H0(omega^2). The five distinct dormant points in
the original curvature equations exhaust the known genus-two candidate
scheme of length five. The chosen horizontal functions satisfy the
actual equation delta^2 f=P f and the exact Wronskian identity 1, so
their matrix is an invertible affine horizontal frame, including at all
finite branch points. It is not just a generic frame with an unexamined
finite degeneracy locus.

Fixing theta=O(O) introduces no additional theta multiplicity. The
usual stability needed by intrinsic incidence also holds here: for the
degree-zero descent W, a line of degree at least zero would pull back
horizontally into the positive HN line theta of F*W (the quotient has
negative degree). This contradicts its nonzero oper second fundamental
map. Tensoring W by a line preserves stability.

For nonzero L in J[3], the degree-two reduced representative D avoids O
and the branch points. L cannot lie in W1 because L(3O)=<1,u>.
The norm description includes repeated support, and its saved exact
discriminant check subsequently rules that out for this curve. The
40 squarefree norm points, with their two choices of a3, give 80 distinct
nonzero classes. They exhaust J[3] since 3 is prime to 5 and genus is 2.
Their Frobenius orbit lengths are 8,24,24,24, while the five opers form
one orbit of length 5. Coprimality gives one product orbit for each
torsion orbit, plus the trivial orbit: 5+40+120+120+120=405 pairs.
Any choice of a root in each product field represents that same orbit.
Galois conjugation preserves geometric existence. Thus five tensors
suffice; the field extensions do not discard nonrational geometric data.

## Twists, finite points, and the point at infinity

Use I(D)=O(D) on the affine curve, in the fractional-ideal convention.
Its k[u]-basis is `1,(v+V)/U`; that of I(-D) is `U,v-V`.
At a support point v=V the former has the permitted pole and the latter
vanishes. At its conjugate, numerator cancellation removes the apparent
denominator; away from these points the modules are trivial. Coprimality
with F checks the branch-point issue. The norm identity gives
`div(h)=3D-6O` for h=v-A, so h generates L^-3 locally everywhere.

For V=V0 L, det V=omega L^2 and the character is tau=L^2.
M=omega^2 L^2. The affine module of Hom(V,M) is I(D), since
L^-1 L^2=L. At O the generator of L=O(D-2O) is q^2, hence V has
transition q^2 G and M has local exponent zero. The untwisted local
regularity test q^4 pG therefore becomes q^2 pG as implemented.
The dual/Frobenius target differs from its base version by L^-3;
consequently j0=h*j0_base is a global isomorphism. At O its local matrix
is exactly `(q^2 G)^[5,t] (h J) GK`, whose negative coefficients vanish
and determinant is a unit. The corresponding untwisted check includes
q^4. These are the correct local types, not missing powers of M.

Dualizing an extension introduces the minus sign. Thus
`D(alpha)=-h^-1 Ji alpha^[5]`, and the exported equation
`I alpha + sum p_i alpha_j^5 tensor_ij=0` has the correct sign.
Column-major flattening agrees with the lattice
`V^vee tensor K`; I inserts each alpha coordinate in the O row of K.
The pairing is `alpha_1 p_2-alpha_2 p_1`, divided by the actual class
of `c/q^3` in H1(omega). This matches u_p=(p_2,-p_1), the fixed
nonzero extension kappa, and normalization ell=1.

## Exact finite-dimensional projection and precision

For a local lattice Q, put a=max(0,-min val(Q^-1)) and
d=max(0,-min val(Q)). Then q^a k[[q]]^r lies in Q k[[q]]^r,
and the latter's image modulo that tail is generated by Q e_i q^j
for 0<=j<a+d. This proves the finite list of local coboundaries used
by Cech, rather than merely observing the expected dimension.

The affine module bases have distinct even/odd leading pole orders.
For I(-D) these are 4+2i and 5+2i, leaving exactly -3,-2,-1,0;
for O they leave -3,-1. Successive leading-term subtraction therefore
gives precisely the implemented scalar reduction. The conceptual bases
continue to all i; the finite implementation needs them only through
pole 80 because every input and local generator is checked to have at
most that pole. Higher local generators lie in the discarded tail.
Consequently the computed 4-dimensional B and 12-dimensional H are
the full cohomology spaces, not selected subquotients.

The global section cutoff is likewise exhaustive: from q^s pG regular,
p=q^-s times a regular row times Gi, so its pole is at most
s+max(0,-min val(Gi)). Here s=4 or 2. The affine Hom module bases
have distinct leading pole orders (0,2,... and 1,3,... in the twisted
case), so every section within this bound is in the listed ansatz.
The kernel imposes every negative local coefficient. Its four rows
are therefore all of A, with no omitted section direction.

The precision argument is not agreement between runs at 500 and 600.
Newton inversion defines u,v in the complete local ring; all subsequent
Laurent operations carry exact absolute precision. Coefficientwise
Cartier extraction sends a known cutoff N to ceil(N/5); Frobenius sends
it to 5N. Inverses and products retain their valuation-adjusted precision.
Regularity and unit-determinant tests need only known negative terms
and the constant term. The finite cohomology projections use exponents
strictly below 10 for the trivial tensor and 8 for every twisted tensor.
The reducers built from expansions through 100 have sufficient precision
even at pole 80 (at least 22 before the projection cutoff); q^a tails
have no effect on the result. Each actual projection asserts its input
precision and pole bound, and coefficient reads assert known precision.
The five saved receipts report maximal input poles 56,48,43,43,43 and
minimum precision margins 34,38,43,43,43. Thus no unknown series tail
can affect an exported coordinate. The separate 500/600 equality is
additional consistency evidence only.

All basis coboundaries are checked to map to zero under I, each of the
four Frobenius/pullback contractions, and the pairing. The exact local
and affine constructions already make these maps global; these checks
confirm the quotient implementation. Neither numerical dimensions nor
the 33-point positive control is used as a substitute for completeness.

## Charts, certificates, and conclusion

The actual backup certificates use the original unrooted incidence
equations, not the reduced rooted presentation. This causes no coverage
gap: under `(p,b)->(t^-4 p,t b)` the first twelve equations scale by t
and ell scales by t^-3. Every normalized solution has b!=0, so its first
nonzero b_j can be set to 1 while retaining ell!=0. The inverse-norm
equation is retained. Conversely ell can be normalized by a cube root.
All four original charts are therefore exhaustive for geometric
existence; no reducedness assertion about these slices is needed.

The seventeen expanded receipts and three factored receipts cover
exactly all twenty (twist,chart) pairs. This audit independently checked
their tensor SHA256 values against the actual five tensor files.
The arithmetic replay itself is accepted from those independent exact
original-row replays, rather than rerunning a solver or every identity.
The factored replay's composition is valid: its identities first give
`1=u*y+u*a^5*c8+u*sum(d_h^5*c_h)+sum((V_l-u*K_l)*f_l)`.
Taking fifth powers and substituting its annihilator identities expresses
1 in the original twelve-row ideal, with polynomial multipliers only.
The thirteenth multiplier is zero. No cancellation by a possibly zero
quantity and no inference from vanishing at finite sample points occurs.
Both recorded negative mutations were rejected for each new certificate.

The audited intrinsic incidence theorem now applies for every oper and
every tau: there is no actual atlas to [H/PGU_3(5)]. A PSU atlas would
compose with the finite etale index-three stack map to yield a PGU atlas,
so is also excluded. This is an endpoint Hermitian-atlas exclusion only.
It does not change the active genus-nine/genus-twenty-five pair, exclude
the other small common-orbifold possibilities, or address arbitrary
coreless common covers. The original unmarked common-cover problem
remains UNSOLVED.
