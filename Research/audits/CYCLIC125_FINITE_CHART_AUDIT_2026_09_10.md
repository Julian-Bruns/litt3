# Cyclic125 finite comparison chart — focused audit

Date: 2026-09-10. Auditor: /root/audit_cyclic125_finite_chart.
Scope: the returned B125 argument, especially its finite geometric
comparison, genuine auxiliary input, and recovery of the specified
truncations. Earlier canonical degree5/25 results, the all-power late
theorem, the mixed-additive norm theorem and the supplied integral
normal Cech splittings are inputs. The root separately replays and
audits the new absorption algebra. No full common-cover verdict is made.

## Verdict

**PASS for B125, after incorporating the finite-chart completion in
Sections 2–4 below into the canonical proof.** The pasted answer's
elimination paragraph is too compressed at a substantive point: local
output Hodge graphs that do not glue cannot be used as preceding LSZ
inputs. A smooth parameter space of genuine global auxiliary opers,
and an integral projection to its scalar coordinates, resolves this
without an extra hypothesis. This is a construction to add to the
proof, not a claim that arbitrary raw graph rescaling is legitimate.

The exact initial value d^5 e also follows, with the partial-solution
argument in Section 6. Applying absorption only to already compatible
T6 would prove a necessary zero-set statement, not by itself evaluate
the obstruction for arbitrary d.

No issue was found with the given-truncation recovery, actual flat
two-torsion line, original cover marking, or the use of the known late
theorem to obtain full towers at125. The audit does not establish a
finite comparison of displacement degree five or higher.

## 1. What the cited constructions actually supply

The full input of higher inverse Cartier includes the previous GLOBAL
filtered flat object and its graded identification. Local filtered and
new graded overlaps must satisfy their actual cocycle conditions before
the corrected tilde overlaps are formed. This is the content relevant
here of [LSZ Theorem4.1 and Lemma4.10](https://arxiv.org/html/1311.6424v4).
It does not construct a higher transform from a nongluing repaired line.

The one-digit normal derivative is coefficient Frobenius followed by
the inverse-Cartier/Hodge projection, as in
[LSYZ Theorem6.2](https://arxiv.org/html/1404.0538v2). Its identification
with Psi is sufficient for the reduction of the final operator L; a
four-digit nonlinear chart is an additional argument, not a quoted
consequence of that derivative formula.

The proof below supplies that additional argument at exactly the finite
precision used for B125.

## 2. Equivariant formal coordinates really are available here

Write G=C125. Choose the descended smooth reference and extend its
underlying curve and original finite etale cover to the precision needed
for a formal chart. There is no demand for a compatible periodic extension
at this step. The reference is a G-fixed point of the marked smooth-curve
deformation space. There are no infinitesimal marked automorphisms.

Let J be the augmentation ideal of its relative formal deformation
ring. Its relative cotangent J/J² is the dual of the integral tangent
lattice. That lattice is free over the Witt deck ring by the stipulated
geometric input; the contragredient of a regular module is again regular.
Thus J/J² is projective as an O[G]-module. The O[G]-linear surjection
J -> J/J² has a section. Its image gives an equivariant map from the
completed symmetric algebra of the cotangent module to the deformation
ring. It induces an isomorphism on cotangents and hence, by formal
smoothness and the formal inverse-function argument, is an isomorphism.

This is stronger than invoking formal smoothness alone. No averaging
by125 or semisimplicity of a characteristic-five representation is used.
One may do this over the finite reference ring and then restrict to the
ball whose curve coordinates are divisible by5^(m+1). Dividing those
coordinates by5^(m+1) gives the x of the comparison, with its specified
leading tangent coordinate unchanged.

The same argument trivializes a finite free equivariant cohomology
bundle in a neighborhood of the reference: split its reduction to the
projective reference fiber, extend the resulting frame over the formal
ring, and apply Nakayama. Source and target Frobenius twists use their
own regular bases. Their abstract deck matrices have integral entries,
and coefficient Witt Frobenius fixes those matrices.

Changing these charts by one inducing the prescribed identity modulo5
does not change the initial d,b coordinates or the terminal class in
D_T after the preceding normal digits vanish.

## 3. Keep genuine global input opers during elimination

Here is the completion needed in the pasted elimination argument.

Over the smooth curve ball use the smooth formal space of GENUINE
projective filtered opers extending the preceding object. It is an
affine torsor in the global scalar coordinates H0(omega²): local scalar
gluing is unobstructed because H1(omega²)=0. Retain the chosen spin data,
graded map and actual flat square-trivial periodicity line. None of
these auxiliary opers is presumed to satisfy the next periodicity
equation. Nevertheless each is a valid higher-IC input.

Integral Serre duality identifies H0(omega²) with the dual of tangent
H1. It is therefore a finite free O[G]-module. The scalar Cech sequence

    0 -> H0(omega²) -> Cech0(omega²) -> Cech1(omega²) -> 0

has an integral deck-linear projection of Cech0 onto H0. One way to
justify this precisely is that a truncated Witt ring is self-injective
and its group ring is a symmetric algebra, hence a finite free O[G]
module is injective. Split the displayed inclusion. On the curve ball,
use compatible equivariant cochain identifications: a pulled-back
affine torsor algebra is projective over the base affine deck algebra,
and the smooth affine base algebra is free over the truncated Witt
coefficient ring. Its underlying deck cochain module is therefore
projective. Lift these identifications successively on the finite
formal parameter ball. The resulting contraction extends by finite
homological perturbation;
the change of the reference boundary is divisible by5^(m+1), and all
required inverses are finite series at the current precision. Equivariance
is preserved. Equivalently, one can construct the split affine torsor
of scalar coordinates along with the preceding formal chart.

For every genuine global input oper and curve parameter, its actual IC
output is defined. In local reference output frames introduce arbitrary
local graph variables u. Normalize those local cyclic vectors by the
unit determinant square root. This produces LOCAL output oper scalars.
The local graphs and these local output scalars are not claimed to glue.
In particular, they are not used as the next preceding filtered object.

Instead project the difference of these local output scalars and the
genuine input oper scalar to the global scalar coordinate space. Impose
equality in this projected space as the scalar equation. The dependence
of the IC output on the preceding scalar gains25 in the corrected
connection; the projected scalar equation is therefore an integral
contracting fixed-point equation in those global scalar variables.
Solve it together with the normal boundary equation and the bijective
cohomology equations. All diagonal inverses are integral and equivariant.

Only then impose the remaining nilpotent normal cohomology equation.
When it vanishes, the normal boundary equation already solved implies
that the output Hodge graphs really glue. Their normalized scalar is
then a GLOBAL projective connection. The projection on H0 is the
identity on global scalars, so the projected scalar equation becomes
the actual periodicity comparison, not merely its projection.

Conversely the supplied compatible upper tuple solves this complete
system. Its genuine preceding global oper and local output graphs
therefore agree with the eliminated variables by the integral uniqueness
of those equations. This proves that the normal equation obtained by
elimination is satisfied by the ACTUAL given upper curve and tuple.
It does not put a Hodge line on an incompatible lower IC output.

At finite height, impose the scalar equation only at the preceding
precision actually read by the next functor. Reduction compatibility
identifies that preceding graph with the relevant reduction of the same
upper graph. Unread top scalar digits have no effect because of the
extra25. No infinite compatible reference or independent choice of a
previous Hodge line has entered the construction.

## 4. Weighted degree of the finite normal equation

Put curve displacement5^(m+1)x, graph displacement5^m u and scalar
displacement5^m R. The exact normal graph equation and the exact cyclic
vector scalar normalization use integral products and unit divisions.
Their homogeneous term with d displacement factors has normalized
valuation at least m(d-1).

The corrected tilde transition uses the new graded diagonal and previous
filtered upper entry. Its scalar dependence is through25r. For Taylor
terms one retains 1/(ell!(j-ell)!), not a factorial without its binomial
numerator. Together with v5(K_j)>=j-1 this preserves the same bound.
Coordinate-composition terms have an extra5 per curve displacement;
the quadratic curve term at m2 is retained, not dropped as square-zero.

The equivariant projections, chart changes, scalar contraction and
ordinary-block inverse are integral additive maps. Solving the auxiliary
equations by finite iteration preserves the bound: composition trees add
the exponents d_i-1 to the number of external factors minus one.
Substitution of a descended constant can lower the external degree but
cannot worsen its valuation; its linear terms belong to L and its
constant terms remain descended.

Coefficient Witt Frobenius and its inverse are additive maps of the
coefficient group, counted as one factor, not as five factors. Products
of their images have the indicated displacement degree. Since only
degrees2,3,4 remain for m1, and degree2 for m2, polarization uses only
units in O. Thus the surviving terms can be expressed as diagonals of
deck-equivariant multi-additive maps at precisely this precision.

Consequently the actual nil equation is

    Lx = Neta +5Q2(x)+25Q3(x)+125Q4(x),       m1;
    Lx = Neta +25Q2(x),                       m2;

with L modulo5 equal to e²Phi. The entire descended auxiliary error
survives as an invariant element of the integral free target, hence as
Neta. Its leading coefficient is the stipulated lower zero-line
obstruction, as follows already from the one-digit equation. In the
compatible initial reference this error is zero.

This proves the finite comparison in the sense needed by B125. It does
not assert that all underlying nonlinear operations vanish, or that the
dangerous separate second repair square is an augmentation image.

## 5. Combined repairs and absorption have the right geometric input

After one coefficient transport y=Phi(x), the first equation reduces
modulo5 to e²y=0. Successive equations through125 give exactly

    y modulo125 in P1+5P4+25P7.

These are restrictions on the actual combined y solving the preceding
normal digits. The second repair as a separate augmentation polynomial
can still have high degree; the complete integral y incorporates the
wrap carries before multiplication. The low polynomial modules used
at each precision are saturated direct summands in the binomial basis,
so the divisions in this deduction are legitimate at the stated degree.

The first full nonlinear expression belongs to F1, and the second-stage
expression belongs to F2 once y modulo25 is in P2+5P4. Applying the
independently checked absorption lemma produces z divisible by5 or25,
respectively. Thus y-z has the same leading tangent coordinate and
satisfies the pure integral norm equation. The norm theorem forces d=0
in the initial stage, and eta0=c=d=0 in the second stage.

At the second stage the reference is built from the ACTUAL recovered
C3, not the old initial C3. Its error remains Neta until norm solubility
forces eta0=0. Therefore the argument obtains compatible C4 rather
than assuming it.

## 6. Exact coefficient: apply the argument to partial solutions

To justify the claimed exact initial d^5 e, not just its zero set, take
a compatible partial upper lift through T5. Its normalized finite
equation is zero modulo125. A smooth terminal curve digit gives the
equation modulo625. The bound on y modulo125 follows from the partial
equations alone. The full nonlinear expression is still absorbed by
Az with z divisible by5. Therefore the actual last normal residue is
the residue of A(y-z), and y-z is a LINEAR partial solution modulo125
with the same leading d,b.

The supplied repaired third linear residue is independent of the free
partial linear repairs and equals -d^5 e. The normal sign is opposite,
so the actual obstruction class is +d^5 e. Changing the smooth terminal
digit contributes im(Psi) and does not change that class. There is one
coefficient Frobenius, and division does not add another.

Existence of the intermediate partial repairs can also be seen in the
same chart: at the first two residual stages their degree bounds are
P2 and P5, and double integration gives P4 and P7. On the zero locus
of the corresponding truncated normal equation the genuine-input
construction of Section3 identifies these with compatible partial
tuples. This is the additional reasoning required if the exact scalar
is recorded, rather than merely the B125 implication.

## 7. Original map, tuple and full towers

Once the leading difference is b e124, it is the pullback of the lower
zero direction. Modify the recovered lower compatible curve in that
direction and lift the ORIGINAL finite etale cover. Injectivity of
pullback on tangent H1 and marked-map uniqueness recover the GIVEN
T3 and then the GIVEN T4, compatibly with h2. Negative normal H0,
projective graded rigidity and the unique square-trivial flat lift match
all supplied data. No unrelated source or revised embedding is used.

The given T6 is sufficient for the first step. The given T7 supplies
the second. For a full tower, B125 gives C4, and the proved late theorem
at n4 with given T8 gives C5. Iteration and the already established
uniqueness/algebraization argument finish full-tower descent at125.

This conclusion is limited to the stated simple-zero/source-defect-two
cyclic125 hypotheses. It is not an exclusion of all common covers and
does not upgrade the all-power late theorem to all-power early descent.

## 8. Separate scoped follow-up: main actual-five-part125 application

Verdict: **PASS** for Section6 of
`Solutions/Sol_cyclic125_bootstrap.md`, in the ordinary-X, source-defect-two,
Galois-Y, nontrivial-five-action branch and ACTUAL Sylow-five order125.
This follow-up accepts the previously audited degree25 application's
deck reduction, canonical-reference and partner-count inputs; the change
of exponent introduces no new endpoint or degree-bound assumption.

The original maps are

    Z -> T ->h Y' ->j C -> Y.

The normal subgroup removed in Z->T has prime-to-five order and acts
trivially on the two defect sections. Thus this step preserves defect2.
The actual group of h is C125, not merely a quotient of the faithful
action on defects. The quotient Y'->Y has degree2 or4, hence genus3
or5. The distinguished C is an original bad double of Y of genus3;
j has degree1 or2, and both Y' and C have defect1. Consequently the
three reduced groups are C250, D250 and C2 times D250, with Dn of
order n.

The bad double's Psi has one zero line and five bijective dimensions.
When j has degree2, normalized trace splits the pulled-back Psi_C
subspace inside Psi_Y'. The complementary subspace has no kernel
because the total defect remains1, and is therefore bijective. This
gives one zero and eleven bijective dimensions in genus5. Thus the
precise simple-zero hypothesis of B125, not just the dimension of its
kernel, is verified.

The canonical ordinary Y tower lifts all these ORIGINAL finite etale
covers and their compatible tuples. Its seventh truncation therefore
supplies the GIVEN reference required by B125, even though the pulled-
back Y' and T connections themselves are nonordinary. Separately, the
canonical ordinary X tower lifts the original X-leg and supplies the
GIVEN compatible full Z tower to be descended. The existing two-leg
canonical-W2 theorem identifies its source marking with the one induced
from Y. No equality of higher canonical endpoint lifts is presumed.

Prime-to-five defect-preserving descent through Z->T, the newly proved
cyclic125 full-tower descent through h, and prime-to-five defect-preserving
descent through j retain the original maps and yield the actual span

    X^can <- Z^can -> C^lift

after algebraization. The X-leg is never replaced by a map from T or
Y'. Both final legs start from the same original-source lift Z^can.
Neither a simultaneous Galois closure nor a lift of C->Y is required.

The final lower partner still has genus3, independent of the125 degree
and of the possible genus5 intermediate. The existing count therefore
remains 80640 times 5^24 times 2^80000000, strictly below
2^80000073 and the unchanged parameter bound K. Stable-model uniqueness
and the Frobenius-stable exceptional parameter set apply exactly as in
the audited degree25 application. No extra factor counting cyclic125
covers is needed: the partner theorem has no bound on either leg degree.

Hence all three specified actual-Sylow125 carrier types are excluded
for the SAME main parameter, in exactly the stated branch. Higher
five-powers, trivial-action carriers, nonordinary-X trace-zero cases,
higher source defects and non-Galois Y legs are not covered by this
application. The unmarked common-cover problem remains unsolved.
