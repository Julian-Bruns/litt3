# Selection: the first primitive wild neutral cover

2026-09-10,22:53CEST. The model-only request is
[PRO_DEGREE5_NEUTRAL_DESCENT_REQUEST.md](PRO_DEGREE5_NEUTRAL_DESCENT_REQUEST.md).
RETURNED after45m24s: no proof of(N5), no actual geometric counterexample.
Focused audit PASS for the new geometric partial results; no request is
outstanding. The model-only file is preserved as the exact sent question.

## User priority and leverage

The latest user asks for a harder shared theorem, aiming for a substantial
Pro research session, and asks root to finish backup degree84/2 meanwhile.
Uniform cyclic (U) and its scoped same-main application are completed
and separately audited. The new target is not another cyclic coefficient.

(N5) asks whether every GIVEN full compatible tower descends along an
ACTUAL degree-five etale map h:T->C with equal positive endpoint defect.
It covers primitive A5/S5 monodromy, arbitrary genus and defect, and
does not assume the Galois closure remains neutral. All original data
and markings are retained. A positive theorem also gives arbitrary
Galois neutral descent through a Sylow tower and the existing prime-to5
theorem. The mechanism applies to both curve choices; neither positive
verdict alone excludes every common cover.

## Why this target, rather than the unchecked broader conjecture

Inventory checked: defect_preserving_etale_descent handles prime-to5
degree, without Galoisness; cyclic_power_early_descent handles one-zero-
line base/source2, with an initial reference. Neither settles neutral
primitive degree5. The known trace-zero regular-module test rules out
another first-order string argument but does not test actual higher flows.

Before drafting, root tested the primitive obstruction explicitly.
On W^5 with S5 action, H=S4, J all-ones, J²=5J and J reduces to e4 on
a five-cycle. The four-dimensional augmentation kernel has both G and
H invariant dimensions1. The actual integral vector(-4,1,1,1,1) is
H-fixed and killed by J Phi, but is not G-fixed. Its25multiple is
G-fixed throughW3 and notW4, while satisfying every later linear equation.

The standard-library check_degree5_neutral_resonance.py replays all
group/subgroup invariants, integral Hecke/norm identities and these
Witt-digit tests in less than0.1s. It constructs NO geometry.
This defeats a broad conclusion from neutral dimensions and arbitrary
linear look-ahead alone. The new question explicitly asks whether the
ACTUAL oper excludes or realizes this minimal primitive norm mode.
The returned answer now proves the needed regular-deck lattice and
splittings for the actual degree-five Galois closure. This does not
exclude the norm model: it embeds into precisely that regular lattice.

## Returned progress and exact remaining step

The actual canonical defect bundle is the cokernel of the normal
p-curvature inclusion T_(C^(1))→F_*T_C. The Bol/Cartier pairing identifies
this cokernel with the already defined canonical-double bundle, not an
unrelated replacement. Under equal endpoint defect, degree-five trace
vanishes on all upper sections. Polarized Serre duality therefore makes
pullback on the primary obstruction cokernel ZERO. The relative Hodge
operator has kernel and cokernel of dimension equal to that defect.

This proves why primary repair upstairs can occur even when lower
compatibility fails. It does not continue that repair through a full
tower. Cyclic-neutral evenness was already known in stronger generality
from symplectic_p_cover_section_growth and is not counted as new.

The missing calculation is the actual higher divided oper residual in
the primitive norm direction, including its coupling to other blocks.
Regularity, kernel/cokernel duality and the Hecke identities all permit
the integral norm example. A new prompt must use the geometric bundle
or higher oper structure beyond those now-settled inputs.

Audit: /root/audit_n5_returned_partial,2026-09-10,PASS scoped additions,
[record](audits/N5_RETURNED_PARTIAL_AUDIT_2026_09_10.md).
Supplied verify_n5.py replays unchanged under SagePython in0.70s.
It verifies finite algebra, not an actual full compatible tower.

## How outcomes change the next action

Positive: neutral degree5 steps and arbitrary Galois neutral maps can
be removed from a given compatible tower with all original maps kept.
This is a new shared descent mechanism and reaches the trivial-action
direction, although other geometric and counting gaps remain.
Negative: an actual curve/connection/full-tower example would show
which closure or oper invariant must be retained. The displayed module
alone is not a negative answer and is not a new common-cover example.

While Pro works, root continues backup84/2. Degree84 primitive3 are now
excluded by the audited dormant-decoration orbit15 theorem. Forty-two
hyperelliptic classes remain. The horizontal product A18*C6 has only
ten linear parameters, tested exactly. Degree2 is the existing
hyperelliptic map on C and requires the X-side degree16 map obstruction.

## Prompt structure

The OpenAI Docs skill was used narrowly for concrete objectives and
follow-through, alongside the user's successful task-list preference:
[official guidance](https://developers.openai.com/api/docs/guides/latest-model).
The mathematical challenge, not a runtime instruction, is intended
to warrant a long research session. No one-hour runtime or successful
verdict is promised. The request contains ONLY model-addressed text,
with established inputs, a tested obstruction, five completion tasks
and precise uses of either verdict. It has about1900words.
