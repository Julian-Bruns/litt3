# Proof: all cubic characters, with original-row unit identities

[Statement](../Theorems/Thm_backup_hermitian_atlas_exclusion.md).
Root2026-09-08, integrating the backup agent's construction and final
one-core certificate completion. Independent major bridge audit PASS.

## 1. Complete finite candidate list

The displayed hyperelliptic polynomial is squarefree of degree5. Hence
g(C)=2, there is one point O at infinity, and div(du/v)=2O.
Fix theta=O(O). The audited rank-two atlas reduction and
`dormant_rank_two_candidates` give exactly five dormant candidates with
scheme length5. The exact original curvature equations have five distinct
solutions in one F125-orbit, so the saved census is exhaustive.

For nonzero L in J(C)[3], its unique effective degree-two representative
D avoids O and the Weierstrass points. Writing D=(U,v-A), U monic
quadratic, the complete norm equation is

    A^2-F=a3^2 U^3,     h=v-A,     div(h)=3D-6O.

Its normalized40-point algebra is reduced; the two signs of a3 give
80 distinct classes, exhausting the prime-to-five group J(C)[3]. Repeated
support is included initially and excluded by an exact discriminant gcd.
Their Frobenius orbit lengths are8,24,24,24. Since these are coprime to5,
the complete405 oper/character pairs have five product orbits of lengths
5,40,120,120,120. No assumption that geometric solutions are rational
over the coefficient field is used.

Exact sources: `scripts/backup_genus_two_prepare.sage`,
`scripts/backup_genus_two_torsion.sage`, and their preparation/torsion JSON
in Research/computations. These check the original equations and lengths.

## 2. These are the full intrinsic atlas equations

For a descent W the oper second fundamental map proves stability:
a nonnegative-degree line in W would pull back horizontally into the
positive oper line of F*W, contradicting its nonzero second fundamental
map. Put V0=W theta and, for L above,

    V=V0 L,       tau=L^2,       M=omega^2 L^2.

Thus det V=omega tau and tau^3=O, exactly as in the audited
`intrinsic_atlas_incidence`. That theorem's full spaces have dimensions
dim A=dim B=4, dim H=12. Its equations are

    F_h(p,b)=(I b)_h+sum_(i,j) p_i b_j^5 T_ijh=0, h=1,...,12,
    ell(p,b)=1.                                                   (1)

Here is why the concrete twisted tensors compute those maps everywhere.
On the affine curve, O(D) and O(-D) have k[u]-bases

    1,(v+Vpoly)/U;                 U,v-Vpoly,

respectively, where Vpoly=A mod U. Cancellation on the conjugate sheet
removes the apparent poles of the first module. At O use q=u^2/v.
The local transition of L is q^2, so that of V is q^2G and that of M
has exponent zero. Consequently all quotient rows satisfy q^2 pG regular.
The untwisted condition is q^4 pG regular.

The fixed identification is j0=h*j0_base, since its target is twisted
by L^-3 and h generates that line. Dualization sends an extension class
alpha to -h^-1*j0_base^-1*alpha^[5], giving the plus sign in(1).
The pairing is (alpha_1 p_2-alpha_2 p_1) divided by the nonzero fixed
extension class in H1(omega). These expressions have the correct affine
modules and local transitions, not just compatible dimensions.

For any local lattice Q, set a=max(0,-min val Q^-1) and
d=max(0,-min val Q). Modulo q^a k[[q]]^r, its image is generated by
Q e_i q^j for0<=j<a+d. Leading-term subtraction in the displayed affine
bases is unique. Thus the implemented finite Cech quotients are ALL of
B and H. Similarly p=q^-s p_local G^-1 bounds every section's pole,
so the four computed quotient rows span ALL of A.

All operations retain rigorous absolute precision. Actual cohomology
cutoffs are10 (trivial) or8 (twisted); maximal input poles are
56,48,43,43,43 and minimum margins34,38,43,43,43. Every coefficient read
asserts known precision, and every basis coboundary maps to zero. Agreement
at precisions500/600 is only an additional check, not the precision proof.
Source: `backup_genus_two_tensor.sage` and `backup_genus_two_finish.py`.

## 3. Exhaustive charts and exact emptiness

For(1) without normalization, scaling (p,b)->(t^-4 p,t b) multiplies F
by t and ell by t^-3. Every normalized point has b!=0. Its first nonzero
b_j therefore gives one of FOUR disjoint charts: earlier b_i=0,b_j=1,
all twelve F_h=0, and z*ell-1=0. Conversely a cube root normalizes ell.
These ORIGINAL UNROOTED charts suffice for geometric existence; no
reducedness claim about these slices is required.

Seventeen charts have expanded polynomial unit identities independently
replayed in `Research/computations/backup_genus_two_atlas_verification.json`.
The remaining chart0 of twists1,2,3 has the following compact exact proof.
Its necessary affine bilinear quotient has length8, certified by sixteen
original row-module identities, twenty-four borders and a commuting
eight-dimensional representation. Its basis is

    1,b3,b2,b1,b3^2,b2*b3,b2^2,b3^3.

The first Frobenius graph is a unit in that quotient. The final proof is
NOT merely a quotient calculation: polynomial identities transfer the
unit back to the original twelve F_h, with multiplier zero on z*ell-1.
For clarity the exact composition works over any characteristic-five
coefficient algebra. Put c=C*F and y=yrow*F. The saved scalar polynomials
f_l,lambda_lh,a,d_h,r,u,K_l,V_l satisfy

    f_l^5(1+c8)=sum_(h<8) lambda_lh^5 c_h,
    b1-y=a^5(1+c8)+sum_(h<8)d_h^5 c_h,
    b1-a^5=r+sum K_l f_l,     1-u*r=sum V_l f_l.

Writing Gamma_l=V_l-u*K_l gives

    1=u*y+u*a^5*c8+u*sum d_h^5*c_h+sum Gamma_l*f_l.

Raise this identity to the fifth power and use the first identity to
replace each f_l^5 by a polynomial combination of c_0,...,c_8.
Since c and y are fixed linear combinations of the ORIGINAL F_h, this
is an explicit polynomial unit identity in their ideal. No nonunit is
cancelled; all powers include variables and coefficients. The separate
checker reads only the original tensor and factored certificate, checks
every coefficient in these identities, and rejects mutations of both
the inverse u and a row multiplier.

Receipts `backup_genus_two_factored_replay_twist1.json`, `...twist2.json`,
`...twist3.json` in Research/computations all PASS. Their certificates
are local-only in the external backup directory; hashes are in the
receipts. Replay takes approximately2–3 seconds per chart on one core.
All twenty original chart ideals therefore contain1 over their exact
coefficient fields, hence remain empty over the algebraic closure.

## 4. Geometric conclusion and limits

Sections1–3 cover every input of the audited intrinsic atlas theorem.
Thus C_alpha has no representable finite etale atlas to[H/PGU_3(5)].
A PSU atlas would compose with the index-three finite etale stack map
to give a forbidden PGU atlas.

For an actual cored span with the fixed X, the audited two large-profile
classification identifies its common stack with one of those two targets.
Both large cases are therefore excluded, without A14/A18 or any further
genus-nine atlas computation. Smaller cored cases and arbitrary coreless
spans remain OPEN. Ordinary endpoint Jacobians and no-Hermitian-atlas
conditions are not asserted preserved by arbitrary common covers.
