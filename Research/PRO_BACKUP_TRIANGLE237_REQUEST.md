# One remaining degree-84 map on the specified genus-two curve

Decide this ONE existence question. The degree24 and degree48 questions
are completed; their results below are inputs, not work to repeat.

## Target

Over k=algebraic closure of F5, choose alpha^3+alpha+1=0 and let

    C: v^2=u(u-1)(u-2)(u-3)(u-alpha).

Does C admit a finite separable degree84 map f:C->P1 with exactly the
three branch values0,1,infinity and COMPLETE fibers

    f*(0)=2D0,    f*(1)=3D1,    f*(infinity)=7Dinf,

where D0,D1,Dinf are reduced, pairwise disjoint, of degrees42,28,12?
Require f to be unramified everywhere else. It need not be Galois.
The question is over ALL k, not maps with coefficients only in F125.

## Established facts — do not reprove

1. Aut_k(C)=C2, generated by the hyperelliptic involution. The geometric
   moduli Frobenius orbit has length3: C,C^(5),C^(25) are pairwise
   nonisomorphic. All listed properties hold on their conjugates.

2. J(C) is ordinary and geometrically simple. Its geometric endomorphism
   algebra is the quartic CM field generated by a root of

       Z^4-8Z^3+182Z^2-1000Z+15625,

   with Rosati-fixed subfield Q(sqrt21). Thus C has no nonconstant map
   to any elliptic curve, and there is no Rosati-symmetric square root
   of5 in End^0(J(C)).

3. C has NO tame maps to P1 with complete uniform profiles
       degree6:  (2,2,3,3),
       degree12: (2,2,2,3).
   The general degree12 theorem actually constructs either an extra
   involution or an INTEGRAL Rosati-symmetric T with T²=[5] from an
   actual bi-etale self-correspondence. Use these conclusions only
   for an ACTUAL intermediate map on C, not after base-changing C.

4. All cyclic etale covers of C of degree1,2,3 or6 have ordinary
   Jacobian. This is not asserted for arbitrary etale covers.

5. The full tame-specialization map for P1(2,3,7) injects its geometric
   characteristic5 cover classes into the characteristic0 permutation
   census, preserving the permutation image, centralizer and block systems.
   It allows groups with order divisible by5; only inertia is prime to5.
   A pair of permutations alone does not establish a map on THIS C.

## Finite structure already available for this new case

The monodromy problem is pairs a,b in S84, modulo simultaneous
conjugacy, with types

                a:2^42,    b:3^28,    ba:7^12.

Keep all branch labels. Every pair with these types is automatically
transitive: an orbit has size divisible by42, but on a42-letter orbit
a would be odd, b even, and ba even, a contradiction.

If a proper invariant partition gives C->B->P1, then B has genus0 or1.
Genus1 is excluded by input2. For genus0, uniformity gives, at a point
whose block-inertia orbit has length ell, the complete inner index e/ell.
Riemann--Hurwitz then leaves only these proper inner degrees/profiles:

    2: (2,2,2,2,2,2);
    3: (3,3,3,3);
    4: (2,2,2,2,2);
    6: (2,2,3,3);
   12: (2,2,2,3).

The degree3 and degree4 actions are respectively C3 and V4, contradicting
Aut(C)=C2. Degrees6 and12 are excluded by input3.
Degree2 is NOT excluded: it is the actual hyperelliptic map. In that
case the outer degree42 map P1->P1 must have complete profiles

                 1^6 2^18,    3^14,    7^6,

and the six simple points of its0-fiber are exactly C's branch divisor.
Thus either a primitive action or this hyperelliptic factor can survive;
do not discard either case without a new argument.

The degree48 certificate is available as reusable code if attached. It
used valid inequivalent tables plus a separately computed character mass
to prove completeness. This is a possible method, not a required one.
For degree84 that mass is

    sum_(lambda partition84)
      H_lambda chi_lambda(2^42)chi_lambda(3^28)chi_lambda(7^12)
    / [(2^42*42!)(3^28*28!)(7^12*12!)].

We have NOT computed this mass or the new census. The degree48 code's
48-letter array and64-bit root mask cannot simply be retained at degree84.
No external character tables or numerical coefficient-field search is
being supplied as a completeness assumption.

## Requested outcome

Give a proof of nonexistence, or an actual map on C with its complete
ramification checked. A successful NO removes the last unknown tame
triangle row of the smaller backup; an actual YES identifies the
precise common orbifold to test against our other endpoint.

Use geometry or exact computation as appropriate. If computational,
include the source, finite witnesses, execution transcript, and a
completeness justification that can be independently replayed. The
geometric bridge to the specified characteristic5 curve is part of the
claim. Do not assume every characteristic0 class has good tame reduction.

The previous Frobenius trick succeeded because at most two permitted
cover classes remained. It is not an assumption for this new passport.
If the verdict is genuinely undecided, state the exact remaining step
briefly; do not substitute another catalogue of weaker necessary
conditions or reprove the supplied exclusions.
