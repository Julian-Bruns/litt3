# J7 Pro response and the stronger conclusion obtained from it

2026-09-08. The returned Pro answer did not decide J7. Its genuinely
useful new input is the contact transformation under the CANONICAL
ramified root lift: I upstairs=7I downstairs-6 at a common zero.

For a jointly minimal orthogonal span with g(Y)=2, m=g(X)-1 and
n=deg(Z/X), its collision identity is

    (1/2)Tr(T^dagger T)=7 tau+6b-3mn(2n-7),

where T is the actual root-Jacobian push-pull, b counts branch pairs
at the zero grid, and tau counts off-grid transverse pairs. It follows
from delta_down=mn(n+1), delta_up=mn(n+28)-q(T), and
delta_up=7delta_down-7tau-6b. Spin descent to the joint field uses
c²,c7 in that field, hence c=c7/(c²)³ in it. These steps are consistent
with the audited root/surface construction; no ambient Hom extension is
needed. The Prym-only factor and degree-divisible-by5 caveats in the
response are real limitations of using only this scalar identity.

The root-Prym support, dimension divisible by3, and prime-to5 nonordinary
image claims use an actual homomorphism, not a tangent-space invention.
They are not needed as dependencies of the theorem below; no separate
independent audit of all the response's Rosati refinements is claimed.

## What changes the conclusion

At a zero, there are at most9 slope classes. Same-slope DOWNSTAIRS
branches have contact at least4, so the canonical root lift has contact
at least22. Therefore a grid point with r branches contributes at least
(5/3)r²-11r upstairs. For any finite reduced union of preserving images,
with total degrees a,b and t=a(g(X)-1)=b(g(Y)-1), this gives

    (5/3)ab-77t <= delta_up <= ab+28t.

The upper bound needs only Hodge index, not Jacobian orthogonality.
Thus ab<=(315/2)t for EVERY finite union. There are finitely many
preserving images; their composition-closed finite etale groupoid gives
an actual coarse core in the original embedded endpoint fields.

This proves a STRONGER statement than J7: the coreless spin-matching
hypotheses themselves are impossible, without ordinary genus2 or Hom0.
Fresh medium audit PASS, including the arbitrary-gcd generalization:
`ramified_root_contact_core`. The compatibility-class distinction in
that audit is incorporated; no unrestricted-union lcm shortcut is used.

The general theorem eliminates genus2 clump sizes6,7,11,12,... as well
as the old size2 case. With the already proved singleton exclusion,
only sizes4mod5 remain. ALL those positive profiles admit a common
regular nilpotent projective connection. The no-clump/empty-connection
alternative remains open. See genus_two_clump_connection_reduction.

Do not repeat J7, spin growth, ordinary-root-Prym disjointness, or a
weight7 shared-section search. That branch is finished. The A18 return
point is ATLAS_REDESIGN.md; its oper census is complete, atlas tests are
not, and they do not test every common dormant connection.
