# Selection record: uniform early cyclic descent

2026-09-10,20:56 CEST. Next prompt is READY, not reported submitted:
[model-only request](PRO_UNIFORM_EARLY_CYCLIC_DESCENT_REQUEST.md).
Tracking and user instructions belong here or in the chat, not inside
that directly copyable request. One manual Pro request at a time.

## Why this is the next useful question

The returned B125 proof is integrated as
[cyclic125_bootstrap](../Theorems/Thm_cyclic125_bootstrap.md).
Both supplied scripts replay unchanged. A focused geometry audit
completed the global auxiliary-oper projection construction, instead
of accepting local nongluing graphs as inverse-Cartier inputs.
The resulting finite comparison and original marked descents pass.
The separate scoped main-pair application also passes: all three
actual-Sylow125 carriers in the ordinary-X/source2/Galois-Y/
nontrivial-five-action branch are excluded for the same parameter.

The root then proved and independently audited
[cyclic_power_nonlinear_absorption](../Theorems/Thm_cyclic_power_nonlinear_absorption.md).
It treats every cyclic power and every relevant nonlinear degree,
including degrees>=5 without division by factorials. Combined integral
repairs, binomial-degree saturation and exact absorption are now known.
Only whole-polynomial equivariance is needed; an individual integral
multilinear presentation need not be equivariant.

Together with the already proved
[all-power late theorem](../Theorems/Thm_cyclic_power_late_descent.md),
this makes an all-power early comparison a bounded, parameterized next
lemma with substantially more leverage than another isolated degree.
The task is uniform in a but limited to one geometric mechanism and
the finite precision needed for its next given truncation.

## Exact target and remaining implication

For q=5^a,a>=4, the new range is 2<=n<=a. Under the actual simple-zero
base/source2 hypotheses, with a GIVEN initial compatible C_(a+3)
reference and full tuple, prove:

    GIVEN compatible C_n and original T_n→C_n,
    GIVEN compatible T_(n+a+1)
       imply GIVEN T_(n+1) descends compatibly to C_(n+1).

The next lower compatibility is a conclusion. Later references may be
incompatible and contribute the ACTUAL norm error. Both original maps
are preserved in the eventual main-pair application; this is not itself
the whole unmarked common-cover theorem.

The proposed route constructs the actual nil equation

    Lx=Neta+sum 5^((n-1)(d-1))Q_d(x)

over W_(a+1), with L mod5=e²Phi and integral d-additive presentations
for the nonlinear terms. Initial reference error is zero. The newly
proved algebra then forces lower compatibility and invariant leading
input without changing the GIVEN upper truncation.

The unresolved implication is the ACTUAL uniform geometric normal
form, not its algebraic solution set. One must retain global input
opers, local output graphs, preceding filtered data, prescribed new
grading, the flat twist, inverse coefficient Frobenius, and every
divided Taylor/Witt term needed at this precision. Degree125 only
needed degrees<=4, where factorial polarization was available.

## What was tested before drafting

[verify_uniform_binomial_absorption.py](../scripts/verify_uniform_binomial_absorption.py)
uses standard-library exact arithmetic. Its latest replay passed:

- 240 coefficient-basis absorption generators over cyclic5,25,125,625;
- 80 nonlinear examples, including quintic terms and noncommuting
  coefficient multiplication/Frobenius over unramified quadratic rings;
- 166650 degree budgets through a=100;
- 125 nonprime-field divided-Frobenius scaling checks.

Total runtime about1.3seconds. These are algebraic diagnostics, not a
simulation of global inverse Cartier. The proof, not the finite tests,
establishes the uniform absorption theorem.

The new geometric question was tested against a concrete potential
failure mode: delta(u)=(Phi(u)-u^5)/5 is pointwise integral, but that
alone does not provide an integral homogeneous presentation. On the
weighted curve input the exact expression is

    delta(5^(m+1)x)=5^mPhi(x)-5^(5m+4)x^5.

This supports the required degree weight while illustrating precisely
why the actual construction must be inspected. It does not establish
the corresponding claim for every Hodge or Taylor operation.

## How either outcome changes the next action

A proof finishes original-map full-tower descent for every cyclic
power using the known late induction and effectivity. It would remove
all remaining nontrivial-five-action cyclic carriers in the scoped
ordinary-X/two-defect/Galois-Y branch, without changing the main pair
or counting individual cover degrees.

An actual finite-level geometric counterexample would identify failed
early descent or an insufficient look-ahead. An actual divided term
violating the normal form would instead identify the operation needing
a broader integral calculus or corrected look-ahead. Neither outcome
alone settles existence of an unmarked common cover.

The separate trivial-action/trace-zero branch remains independent;
while Pro runs, continue its actual cored-map/function geometry rather
than duplicating this uniform comparison.

## Prompt construction

The inventory, new proof and exact diagnostics were checked first.
The request supplies the stronger all-power algebra and late theorem,
and the corrected degree125 global-input construction as established
inputs. It leads with the mathematical opportunity, then gives one
target and a concrete completion list. The closing prioritizes quality
and follow-through without an artificial duration or runtime guarantee.

The OpenAI Docs skill was used for prompt structure, with the official
[model prompting guidance](https://developers.openai.com/api/docs/guides/latest-model)
as a narrow reference. The user's earlier task-list preference and
firsthand prompting notes remain in force; no claim is made that a
phrase guarantees a longer or more successful run. The mathematical
selection comes from the new local results, not prompting folklore.
